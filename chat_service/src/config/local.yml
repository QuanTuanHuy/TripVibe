app:
  datasource:
    driver: postgres
    host: localhost
    port: 5433
    database: db_chat_service
    username: user_booking
    password: secret
  redis:
    host: localhost
    port: 6379
  logging:
    development: true