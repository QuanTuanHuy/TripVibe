app:
  datasource:
    driver: postgres
    host: localhost
    port: 5433
    database: db_notification_service
    username: user_booking
    password: secret
  redis:
    host: localhost
    port: 6379
  kafka:
    bootstrapServers: localhost:9092
    consumer:
      groupId: notification_service
  logging:
    development: true

  services:
    email:
      apiKey: xkeysibtest
      url: https://api.brevo.com
    internal:
      - name: profile
        uri: http://localhost:8081
        contextPath: /profile_service
